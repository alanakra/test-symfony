{% block body %}
<div>
<h1>Bonjour {{ app.user.name }} {{ app.user.lastname }}</h1>
<a href="{{ path('logout') }}">Me déconnecter</a>
</div>
    {% if is_granted('ROLE_ADMIN') %}
      <a href="{{ path('user_index') }}">List Users</a>
      <a href="{{ path('user_new') }}">Create Users</a>
      <a href="{{ path('bank_account_index') }}">Compte bancaires</a>
    {% endif %}
    <div>
    </div>
{% endblock %}